# Buildfiles configuration

# Define toolchain build (target)
TARGET_TOOLCHAIN="host/crosstool-ng"

# Define sysroot (target)
TARGET_SYSROOT_LINK="$HOST_SYSROOT/arm-unknown-linux-gnueabi/sysroot"

# Define target system
TARGET="arm-unknown-linux-gnueabi"

# Define architecture
ARCH="arm"

# Add toolchain to path
PATH="$HOST_SYSROOT/bin:$PATH"

