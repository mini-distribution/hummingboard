# Buildfiles configuration

# Define host toolchain build
HOST_TOOLCHAIN="build/crosstool-ng"

# Define host sysroot link
HOST_SYSROOT_LINK="$BUILD_SYSROOT/arm-unknown-linux-gnueabi/sysroot"

# Define host system
HOST="arm-unknown-linux-gnueabi"

# Add host toolchain to path
PATH="$BUILD_SYSROOT/bin:$PATH"
PATH="$BUILD_SYSROOT/usr/bin:$PATH"

