# Description: A minimal filesystem
# URL:         None
# Maintainer:  Martin Lund <martin@martinlund.org>

name=fs
version=1.0
release=1
depends=(target/busybox)

build() {
	cp -a $BG_SYSROOT_DIR/* $PKG
	
	mkfs.jffs2 -r $BG_SYSROOT_DIR -e 64 -o $BUILD/fs.jffs2
}
